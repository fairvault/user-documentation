source "https://rubygems.org"

# Lock to the GitHub Pages build so local = production
gem "github-pages", group: :jekyll_plugins
gem "webrick" # needed on Ruby 3+

# Optional: add more Jekyll plugins here (must be whitelisted by GitHub)